# Planning doc (kept local, not tracked)
PRD.md

# Secrets / local config (committed as *.example instead)
apps/daemon/.env
apps/watch/source/Config.mc

# Node
node_modules/
dist/
*.log
.DS_Store

# Connect IQ build artifacts
*.prg
*.prg.debug.xml
apps/watch/bin/

# Cloudflare tunnel local state (keep config.example.yml tracked)
tools/tunnel/config.yml
tools/tunnel/*.json
tools/tunnel/cert.pem
